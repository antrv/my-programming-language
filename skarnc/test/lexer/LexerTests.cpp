#include <gtest/gtest.h>
#include "lexer/Lexer.h"

TEST(LexerTests, Example1)
{
    // using namespace skarn::lexer;
    // constexpr std::string_view ident {"name123_bbb123DDDx"};
    // const auto result1 = parse(ident);
    // ASSERT_TRUE(result1);
    // EXPECT_EQ(result1.value().unitName, ident);
    //
    // const auto result2 = parse("let");
    // ASSERT_FALSE(result2);
    // ASSERT_EQ(result2.error().size(), 1);
    // EXPECT_EQ(result2.error()[0].message, "error");
}
